class Rectangle {
    constructor(height, width) {
      this.height = height;
      this.width = width;
    }
  }

  class Point {
    constructor(x, y) {
      this.x = x;
      this.y = y;
    }

    class Animal { 
        speak() {
          return this;
        }
        static eat() {
          return this;
        }
      }
      lass Rectangle {
        constructor(height, width) {
          this.height = height;
          this.width = width;
        }
      }
